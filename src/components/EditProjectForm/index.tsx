import React from 'react';

import styles from './styles.module.scss';

type Props = {};

const EditProjectForm = (props: Props) => {
  return (
    <div className={styles.editProjectFormWrapper}>
      <p>EditProjectForm</p>
    </div>
  );
};

export default EditProjectForm;
